class GreatestCommonDivisor
{
	int aa = 10;
	int bb = 20;
	boolean res = true;

	void main()
	{
		int x, y, z;
		int a = 98;
		x = 10;
		y = bb;
		z = (x + y) * 10 / 2;//gcd(x, y);
		print z;
	}

	// Funcion que calcula el maximo comun divisor
	int gcd(int a, int b)
	{
		boolean test;
		int x = 8;
		x = 1000;
		test = true;

		if (b == 0) {
			return (a);
		} else {
			return (gcd(b, a % b));
		}
		print "x: ",x;
		print "";
	}
}
