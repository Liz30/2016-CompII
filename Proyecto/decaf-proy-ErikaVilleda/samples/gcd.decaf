class GreatestCommonDivisor
{
	int a = 10;
	int b = 20;
	boolean res = true;

	void main()
	{
		int x, y, z;
		x = a;
		y = b;
		z = 60;//gcd(x, y);

		print z;
	}

	// Funcion que calcula el maximo comun divisor
	int gcd(int a, int b)
	{
		boolean test;
		int x;
		aa = 9;
		bb = y;
		x = 1000;
		test = true;
		test = a;
		if (b == 0) {
			return (a);
		} else {
			return (gcd(b, a % b));
		}
	}
}
